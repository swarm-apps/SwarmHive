# docker-compose 的可覆盖变量。复制为 .env（已被 .gitignore 忽略）后按需修改。
# 不设则使用 compose 内的默认值；本地开发通常无需任何改动。

# ---- bundled-storage profile (RustFS) ----
# ⚠️ rustfsadmin/rustfsadmin 仅供本地开发。生产部署务必替换为强随机密钥
#    （如 `openssl rand -hex 24`），否则任何人可读写你的对象存储。
SWARMHIVE_RUSTFS_ACCESS_KEY=rustfsadmin
SWARMHIVE_RUSTFS_SECRET_KEY=rustfsadmin
# server storage backend 指向的 bucket，rustfs-init 会自动建好它（无需手动创建）。
SWARMHIVE_RUSTFS_BUCKET=swarmhive
