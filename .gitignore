# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node / pnpm
node_modules
dist
dist-ssr
build
.cache
.vite
.turbo
*.local
*.tsbuildinfo

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust
target/
**/target/**
Cargo.lock.bak

# Env
.env
.env.local
.env.*.local

# Tooling caches
.eslintcache
.biome-cache

# OS / misc
**/.obsidian

# SwarmHive runtime data (local dev)
data/
storage/
tmp/

# Per-developer config overrides (config/default.toml is committed; this
# layer is for secrets and per-machine tweaks like a different DB port).
config/local.toml
config/*.local.toml

# Claude Code — ignore everything by default, then reverse-allow the bits we ship
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/dev-workflow/
!.claude/skills/dev-workflow/**
!.claude/settings.json

# OpenSpec is project-tracked except local archives
**/openspec/**/archive/local/
